services:
  - type: web
    name: today-tomorrow
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node push-server.js
    healthCheckPath: /health
    autoDeploy: true
